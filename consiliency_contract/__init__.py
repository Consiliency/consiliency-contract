"""consiliency-contract (npm: ``@consiliency/contract``) — provisioning placeholder.

The normative contract content (the ``.consiliency/`` layout + manifest schema, the
archetype registry, required-doc sets, the interface-declaration schema, the loop-gate
protocol, ``canonical_html.v1``, and the version-skew protocol) lands in ``0.1.0`` as
shared JSON data + conformance vectors, in byte-parity with the npm reader.
"""

CONTRACT_PACKAGE = "consiliency-contract"
STATUS = "placeholder"
__version__ = "0.0.1"
