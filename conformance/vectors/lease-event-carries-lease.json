{
  "id": "lease-event-carries-lease",
  "description": "An acquire lease-event carries the full lease snapshot. Exercises the lease-event -> lease cross-file $ref (the event's lease field validates against lease.schema.json by $id).",
  "input": {
    "schema": "consiliency.lease_event.v1",
    "event": "acquire",
    "lease_id": "lease-src-mod",
    "holder": "agent-a",
    "at": "2026-01-01T00:00:00Z",
    "lease": {
      "schema": "consiliency.lease.v1",
      "lease_id": "lease-src-mod",
      "holder": "agent-a",
      "acquired_at": "2026-01-01T00:00:00Z",
      "ttl_seconds": 300,
      "heartbeat_at": "2026-01-01T00:00:00Z",
      "mode": "soft",
      "scope": {"granularity": "path-set", "selector": ["src/mod/**"]},
      "phase": "implement"
    }
  },
  "decision": {
    "schema": "consiliency.conformance_decision.v1",
    "status": "accepted",
    "maturity": "presence-only",
    "findings": [{"code": "coordination.lease_event_recorded", "severity": "info", "message": "Append-only acquire event carries the full lease snapshot; the current-lease view is the projection of such events."}]
  }
}
